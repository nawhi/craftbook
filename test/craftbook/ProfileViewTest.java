package craftbook;

import static org.junit.Assert.*;

import org.junit.Test;

public class ProfileViewTest {

	/**
	 * ProfileView should calculate: 
	 * 
	 * - display nothing if user has no posts
	 * - display posts in chronological order
	 * - display a time as 'ago' from the current time
	 * 
	 * testing note: it might have been neater to construct the
	 * test view objects with a mock PrintStream, rather than
	 * testing calculate() separately and leaving show()
	 * untested
	 */
	
	@Test
	public void test() {
		fail("TODO");
	}
	
}
